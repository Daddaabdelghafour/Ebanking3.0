name: Auth Service CI

on:
  push:
  pull_request:

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up JDK 21
      uses: actions/setup-java@v3
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven
    
    - name: Build with Maven
      working-directory: ./auth-service
      run: mvn clean package -DskipTests
    
    - name: Run tests
      working-directory: ./auth-service
      run: mvn test
    
    - name: Log in to GitHub Container Registry
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Build Docker image
      run: |
        docker build -f Dockerfile.auth-service -t ebank/auth-service:${{ github.sha }} .
        docker tag ebank/auth-service:${{ github.sha }} ebank/auth-service:latest
    
    - name: Tag for GHCR
      run: |
        REPO_LC=${GITHUB_REPOSITORY,,}
        docker tag ebank/auth-service:${{ github.sha }} ghcr.io/${REPO_LC}/auth-service:${{ github.sha }}
        docker tag ebank/auth-service:${{ github.sha }} ghcr.io/${REPO_LC}/auth-service:latest

    - name: Push to GHCR
      if: github.event_name != 'pull_request'
      run: |
        REPO_LC=${GITHUB_REPOSITORY,,}
        docker push ghcr.io/${REPO_LC}/auth-service:${{ github.sha }}
        docker push ghcr.io/${REPO_LC}/auth-service:latest

    # TODO: Add deployment step (CD) - Responsible: Abdelghafour
    # - name: Deploy to Kubernetes
    #   run: |
    #     kubectl set image deployment/auth-service auth-service=ebank/auth-service:${{ github.sha }} -n ebank
