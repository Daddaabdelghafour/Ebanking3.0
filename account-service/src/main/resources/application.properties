spring.application.name=Account-service

server.port=9090
server.url=http://127.0.0.1:${server.port}

# Database Configuration
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=update

# Bank code constants
bank.bank-code=026
bank.city-code=001
bank.currency=MAD
bank.account-number-initial-value=30122025

# JWT Configuration
application.security.jwt.secret-key=ss/y46y8UJfCO9SOHSQry0VA3+LqnxFWWAzHU8BRpQA=
application.security.jwt.expiration=300000
application.security.jwt.refresh-token.expiration=2592000000
# Email Configuration
spring.mail.protocol=smtp
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=ishaq.haj2@gmail.com
spring.mail.password=rltrlzanlysmqdfe
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Frontend URL
application.port=3000
application.frontend.domain=http://localhost
application.frontend.url=${application.frontend.domain}:${application.port}

# The path to store uploaded video training
application.video.uploaded.path=C:\Users\Ishak\ishaq\Projects\ProjectFullStack\training_video

# File Upload Configuration
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB
spring.servlet.multipart.enabled=true

# Local AI Service Configuration (No API Key needed)
ai.service.url=http://localhost:5000/api/analyze-video
ai.service.timeout=300000
ai.service.max-retries=3